{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "packageManager": "yarn@4.9.4",
  "engines": {
    "node": "20.18.x"
  },
  "scripts": {
    "dev": "yarn workspace web dev",
    "build": "yarn workspaces foreach -Rv run build",
    "typecheck": "yarn workspaces foreach -Rv run typecheck",
    "lint": "yarn workspaces foreach -Rv run lint",
    "test": "yarn workspaces foreach -Rv run test",
    "gen:entities:debug": "tsx scripts/generator/entry.ts --debug"
  },
  "devDependencies": {
    "vite-tsconfig-paths": "^5.1.4"
  }
}
